# NEXT-STEPS.md

1. Run `pnpm install`, `pnpm db:generate`, `pnpm db:migrate --name init`, `pnpm db:seed`.
2. Configure Google OAuth, OpenAI, Resend, MCP token in `.env.local`.
3. Verify acceptance scenarios in PRD section 17.
4. Deploy to Vercel and set production env vars.
